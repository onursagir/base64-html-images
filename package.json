{
  "name": "base64-html-images",
  "bin": "./dist/cli.mjs",
  "main": "./dist/cli.mjs",
  "tags": [
    "base64",
    "html",
    "images"
  ],
  "description": "Replace images in HTML file with base64 string",
  "version": "1.0.3",
  "license": "MIT",
  "scripts": {
    "dev": "tsup ./src/cli.ts --watch --format esm",
    "build": "tsup ./src/cli.ts --format esm",
    "prepublish": "chmod +x ./dist/cli.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "@types/yargs": "^17.0.24",
    "prettier": "^2.8.8",
    "tsup": "^6.7.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "parse5-html-rewriting-stream": "^7.0.0",
    "yargs": "^17.7.2"
  }
}
